Boost.Uuid (and minimal dependencies) — Boost 1.86.0
Extracted from vcpkg package boost-uuid:x64-mingw-dynamic (header-only).
Includes: uuid, config, assert, static_assert, throw_exception, type_traits, etc.

To refresh:
  third_party/vcpkg/vcpkg install boost-uuid:x64-mingw-dynamic
  copy installed/.../include/boost -> third_party/boost_uuid/boost
