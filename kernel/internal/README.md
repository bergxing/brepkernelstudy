# Private kernel headers (NOT on the PUBLIC include path).
#
# Add implementation-only headers here, e.g.:
#   kernel/internal/brep/internal/foo_detail.hpp
# Include as: #include "brep/internal/foo_detail.hpp"
#
# Clients (Viewer, examples, tests) must not include these; see
# scripts/check_include_boundaries.py and ADR 0002.
