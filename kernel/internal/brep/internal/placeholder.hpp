#pragma once

// Placeholder so the PRIVATE include root is non-empty and discoverable.
// Replace / extend with real implementation-only headers as needed.
// Clients must not include this path (enforced by check_include_boundaries.py).

namespace brep::internal
{

inline constexpr bool k_internal_include_root = true;

}  // namespace brep::internal
